# Revision instructions (v1)

An independent reviewer critiqued your draft. Produce a revised version of
the SAME document (same doc_type, same JSON structure, same citation rules).

Rules:
- Address EVERY mandatory fix listed below. Apply suggestions only where they
  don't conflict with the hard rules.
- All original hard rules still apply — especially: use ONLY the provided
  profile content, cite supporting chunk_ids on every unit, never invent
  facts. A reviewer fix is NEVER a licence to fabricate; if a fix demands
  evidence the profile doesn't contain, satisfy it as best you can with real
  content or leave that point out.
- Keep what the reviewer didn't flag; don't rewrite for the sake of it.
